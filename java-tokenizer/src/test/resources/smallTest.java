package org.apache.commons.lang;

import java.util.ArrayList;

import org.junit.Test;

public class smallTest extends MathClass<String> implements Berndddd<Integer>, Hanssss<Double,String> {
	
	private static int global = 3;
	final private int global2[] = Carrier.array;

	@deprecated
   public smallTest(){
	   super();
   }
   
	@sdsdsd("bla")
   public int doSth(float number) {
	   int i = 0;
	   if (i == 3) {
		   ++i;
	   } else {
		   --i;
	   }
	   switch(i) {
	   case 1:
		   break;
	   case 0:
		   break;
	   }
	   for (int v = 1; v < 12; v = v + 1) {
		   double hihi = (double) 7.0;
		   Double zDouble = new Double("3.0", 8);
	   }
	   for (String element : liste) {
		   i++;
	   }
	   Math.global = someBooleanvalue ? new House(1) : new House<String>(12);
	   assert b == "rot";
	   House[] array = { (someBooleanvalue ? new House(1) : new House<?>(12))  };
	   Boolean[][] arr = new Boolean[3][12];
	   int[] intArray = { (int)2, 3, 4, someArray[5] };
	   return Math.out.log(i);
   }

}
